# agent-frameworks/nova-code-review-graph

Knowledge graph-based code review agent. Parses codebases via Tree-sitter, builds a structural graph in SQLite, exposes compact context to AI tools via MCP.

Repo: `c10vis-poem/nova-code-review-graph`

If structural ties to the memory layer emerge (e.g., graph stored in OB1), link will be added to CLAUDE.md and README.
