# agent-frameworks/nova-code-review-graph — LLM Wiki

## What this is

Token-efficient code review via a persistent, incrementally updated knowledge graph. Tree-sitter parses the codebase; SQLite stores the structural graph; 30 MCP tools expose compact context to AI coding assistants.

## Repo

`c10vis-poem/nova-code-review-graph`

## Memory layer tie

TBD — if the graph is eventually stored in OB1/pgvector, this entry will be linked from the memory layer docs.
