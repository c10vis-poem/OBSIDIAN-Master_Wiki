# mcp-servers — LLM Wiki

## What this is

Documentation for every MCP server in the system. Each entry covers: what tools it exposes, its endpoint address, how it's loaded under AESOP XI, and which agents can reach it.

## Known servers

- `ecc/` — error correction and context continuity (built-in)
- Additional servers TBD as the stack matures
