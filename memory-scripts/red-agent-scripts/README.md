# memory-scripts/red-agent-scripts

Scripts used by the red-agent-auditor in its sandboxed audit pass. Receive the same tools as files-inference-node to independently verify the manifest before it reaches the editor.
