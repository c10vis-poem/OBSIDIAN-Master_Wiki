# memory-scripts/memory-nano-agent

Memory and files nano-agent. A small dedicated agent for memory operations: reading/writing to OB1, triggering mem0 updates, file system management tasks that don't need the full agent loop.
